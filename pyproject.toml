[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyfemat"
version = "0.1.0"
description = "FEMAT — Python interface"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["pyfemat"]
